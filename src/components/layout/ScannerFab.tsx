// FAB removed: the central "Escanear" button in BottomNav is now the hero action.
// Kept as no-op export so existing imports don't break during the migration.
export function ScannerFab() {
  return null;
}
